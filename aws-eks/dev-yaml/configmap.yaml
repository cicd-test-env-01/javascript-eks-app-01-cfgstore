apiVersion: v1
kind: ConfigMap
metadata:
  name: javascript-eks-app-01-configmap
  namespace: cicd-test-env-01
data:
  db.properties: |-
    {
      "Database": {
        "dbUser": "dev-username",
        "dbPassword": "dev-password"
      }
    }
